# 心理沙盘互动体验

一个本地运行的单页前端应用。用户可以在虚拟沙盘中选择、拖拽、摆放沙具，写下作品标题、当前感受、关键词和自述文本，然后主动保存截图、导出 JSON，或复制提交给分析 Agent Skill 的说明文本。

本项目用于自我表达与情绪觉察，不提供心理诊断、心理治疗或医疗建议。

## 如何运行

直接用浏览器打开 `index.html` 即可运行：

```text
sandplay-interactive/index.html
```

也可以在项目目录启动静态服务器：

```bash
python -m http.server 8000
```

然后访问 `http://localhost:8000`。基础功能不依赖后端、不依赖构建工具、不需要真实 OpenAI API。

## 功能列表

- 欢迎/引导模态框，包含玩法说明、安全边界和免责声明。
- 左侧沙具库，支持分类筛选、搜索、点击添加和拖拽添加。
- 中央虚拟沙盘，支持选中、移动、键盘微调和触控指针操作。
- 对象属性：名称、类别、位置、大小、旋转、层级、水平翻转。
- 对象操作：缩放、旋转、翻转、复制、删除、置顶、置底。
- 顶部工具栏：撤销、重做、重置、保存 PNG、导出 JSON、复制分析 Agent 文本。
- 右侧作品信息：作品标题、当前心情、关键词、自述文本。
- 轻量引导面板：在用户没有方向时提供一个温和起点和一句观察记录。
- PNG 截图只导出沙盘区域。
- JSON 导出包含作品文本和沙盘对象布局。
- 不自动上传截图、文本或布局数据。

## 替换 ImageGen 正式资源

当前前端使用 `assets/generated/` 中的 ImageGen 生成 PNG 资源。`assets/placeholders/` 中的 SVG 仅作为 fallback/reference 保留。

替换步骤：

1. 使用 `imagegen-prompts/style-guide.md` 中的统一风格要求生成透明背景 PNG。
2. 将生成的资源放入 `assets/generated/` 或新的资源包目录。
3. 更新 `assets/manifest.json` 中对应物件的 `placeholderSrc`。
4. 如需让前端直接使用更多正式资源，可在 `app.js` 的 `ASSETS` 数组中增加条目或改为加载 manifest。

## 保存截图

点击顶部工具栏的 `保存 PNG`。文件名格式为：

```text
sandplay-YYYYMMDD-HHMMSS.png
```

截图只包含沙盘区域，不包含侧栏、工具栏或作品文本。

## 复制给分析 Agent

点击 `复制给分析 Agent` 后，应用会复制一段中文提交文本，包含：

- 作品标题
- 当前心情
- 关键词
- 用户自述
- 引导提示和用户的一句观察
- 希望分析 Agent 关注的方向
- 安全边界
- 截图需由用户主动上传的说明
- 可选布局 JSON 摘要

用户需要自行将截图和文本提交给预设分析 Agent Skill。网页不会自动发送给任何服务。

## 安全与隐私说明

- 本工具不是心理诊断、心理治疗或医疗建议工具。
- 任何后续分析只能用于自我反思和心理教育。
- 若用户出现自伤、伤人、严重痛苦或危机风险，应联系当地紧急服务、危机热线、专业心理工作者或身边可信任的人。
- 图片、文字和布局数据默认只在本地浏览器中处理。
- 用户需要主动下载截图、导出 JSON 或复制文本。
- 前端不会自动上传作品，也不会收集真实姓名、联系方式、年龄、病史等敏感信息。

## 已知限制

- 当前前端使用 32 个 ImageGen PNG 沙具；`assets/manifest.json` 已覆盖完整 44 个物件规划，其中未在前端展示的物件仍待继续生成正式 PNG。
- 不同沙具已有不同默认尺寸和缩放范围，例如树、山、房屋、城堡会比人物更大，鸟、灯、心形等会更小。
- 选中沙盘对象后，可在沙盘区域使用鼠标滚轮缩放；按住 Shift 滚动会使用更细的缩放步进。
- 前端内置了一组可用沙具；后续可改为动态加载完整 manifest。
- 没有后端会话历史，刷新页面会丢失当前作品。
- 移动端支持基础操作，但复杂排布更适合平板或桌面浏览器。
- 撤销/重做保存在内存中，不会跨页面刷新保留。

## 后续可扩展方向

- 接入真实 ImageGen API 批量生成资源。
- 增加多套风格资源包。
- 增加团体协作模式。
- 增加本地会话历史。
- 增加主持人模式。
- 增加多语言。
- 增加无障碍支持。
- 增加移动端优化。
